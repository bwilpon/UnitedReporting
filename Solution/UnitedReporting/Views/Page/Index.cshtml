@model UnitedReporting.Model.Page

@if (Model.DisplayImage)
{
	<section class="featured">
		<img src="@Model.ImageUrl" />
	</section>
}

<article>
	<hgroup class="title">
		<h1>@Model.Name.</h1>
		<h2>@Model.SubTitle.</h2>
	</hgroup>
	@Html.Raw(Model.Body)
</article>